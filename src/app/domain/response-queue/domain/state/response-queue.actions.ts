import { createAction, props } from '@ngneat/effects';
import { TradeEvents } from '../../../gateway/domain/models/gateway.model';

export class ResponseQueueActions {
    // public static readonly tradeEvent = createAction('[responseQueue Store] accept Trade', props<TradeEvents>());
}